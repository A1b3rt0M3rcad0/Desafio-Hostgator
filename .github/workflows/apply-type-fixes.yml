name: Apply type fixes

on:
  push:
    branches:
      - refactor/analytics-use-case-repositories
    paths:
      - scripts/apply_type_fixes.py
      - .github/workflows/apply-type-fixes.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/analytics-use-case-repositories
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Apply deterministic fixes
        run: python scripts/apply_type_fixes.py
      - name: Remove one-shot automation
        run: |
          rm scripts/apply_type_fixes.py
          rm .github/workflows/apply-type-fixes.yml
      - name: Commit fixes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet && exit 0
          git commit -m "fix: resolve analytics type diagnostics"
          git push origin HEAD:refactor/analytics-use-case-repositories
